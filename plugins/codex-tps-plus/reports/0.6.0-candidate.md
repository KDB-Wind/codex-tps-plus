# 0.6.0 candidate verification — 2026-09-07

Candidate code: `cb294b6a3a0d3bad4ebf44989fcd3281209c1635`, branch
`release/0.6.0-candidate`. The production Hook files are unchanged from `04945ec`.

## Automated evidence

[GitHub Actions run 34089731796](https://github.com/KDB-Wind/codex-tps-plus/actions/runs/34089731796)
passed all six combinations of Windows, macOS, Linux and Node.js 22, 24. Each combination ran:

- All 98 tests, including the four accuracy regressions and candidate/release gate checks.
- Candidate package, Hook, version, and tracked-file checks.
- Real Codex CLI 0.153.4 installation smoke in a disposable CODEX_HOME: install 0.5.0, upgrade to
  0.6.0, preserve numeric state, backfill completion, repeat Stop, recover after removing the old
  cache, uninstall, and install the candidate afresh.

The initial matrix exposed a synthetic probe test using wall-clock timestamps for notifications
delivered in one millisecond. The test now uses an injected deterministic metric clock and ends on
the completed event. Production zero-duration validation was not relaxed.

## Local trial installation

Installed `0.6.0+codex.20260907060839` in both the standard Codex home and the active desktop runtime
home through the existing personal marketplace. Verified all 20 production Hook/script/skill files
against the source. Both homes have an active stable runtime snapshot:

`157713bb3833cb6beecd894b48df480ab4cb5955b3e95b9398f275210233459e`

Doctor reports zero failed checks for each home. The source manifest remains the unsuffixed `0.6.0`
so the trial cachebuster does not enter distribution metadata. Existing numeric session data was
retained. No main update, release tag, or GitHub Release was published.

## Remaining human trial

The automated smoke calls the installed shell Hook definitions with synthetic inputs. It makes zero
model requests and does not prove real TUI event scheduling or UI presentation. Start a new session
to load the installed revision, then cover ordinary replies, a tool-heavy turn, a long session,
concurrent sessions, and a resumed session. Check that throughput appears, completion timing remains
stable after backfill, TTFT labels are correct, and no Hook error or sustained resource spike appears.

After several days of normal use without a regression, rerun the matrix on any later code changes
before promotion. `npm run release:verify` intentionally requires an actual matching local tag; its
positive and negative behavior is tested in temporary Git repositories without tagging this release.
